<%= form_for(@reboot) do |f| %>
  <% if @reboot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reboot.errors.count, "error") %> prohibited this reboot from being saved:</h2>

      <ul>
      <% @reboot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :type %><br>
    <%= f.number_field :type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
